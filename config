#!/usr/bin/env bun

import { $ } from "bun";

const command = process.argv[2];

if (command === "brew") {
  await $`brew update`;
  await $`brew bundle install --global --cleanup --zap --upgrade`;
} else {
  console.log("Usage: config <command>\n");
  console.log("Available commands:");
  console.log("    brew - Run brew bundle install");
  process.exit(1);
}
